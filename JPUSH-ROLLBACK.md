# 🔙 极光推送回退说明

**回退时间**：2026-06-22 00:19  
**回退原因**：极光推送方案复杂度高，收益不明显

---

## ❌ 极光推送方案的问题

### 1. 复杂度过高
- 需要 10+ 个厂商推送 SDK（华为、VIVO、小米等）
- APK 体积从 6.1 MB 膨胀到 16 MB
- 需要配置多个权限（WAKE_LOCK、电池优化、自启动）

### 2. 华为设备限制严格
- App 杀掉后难以自启动
- 需要用户手动配置多项权限
- 即使配置了也可能被系统杀掉

### 3. 可能阻止系统休眠
- WAKE_LOCK 权限会保持唤醒
- 影响平板续航
- 用户体验不如 WorkManager

### 4. 收益不明显
- WorkManager 15 分钟轮询已经够用
- 只要联网，15 分钟内必定能收到通知
- 简单可靠，不需要用户额外配置

---

## ✅ WorkManager 方案的优势

### 1. 简单可靠
- 只需要通知权限（自动申请）
- 不需要电池优化、自启动等复杂权限
- Android 系统保证执行

### 2. 省电
- 不阻止系统休眠
- 不需要 WAKE_LOCK
- 批量调度，系统优化

### 3. 稳定
- 15 分钟轮询非常稳定
- 即使 App 被杀掉，系统会重新调度
- Doze 模式下也会执行（虽然可能延迟）

### 4. 体积小
- APK 只有 6.1 MB
- 没有厂商 SDK 依赖
- 代码简洁

---

## 📦 当前稳定版本

**版本**：v2026.06.21.24  
**APK 路径**：`android/app/build/outputs/apk/debug/app-debug.apk`  
**APK 大小**：6.1 MB  
**构建时间**：2026-06-22 00:19

### 功能清单
- ✅ WebView 强制禁用缓存（确保加载最新网页）
- ✅ 微聊 UI 优化（孩子端输入框可见）
- ✅ 历史消息连续加载（滚动位置精确恢复）
- ✅ 语音发送功能（原生录音）
- ✅ 图片上传功能
- ✅ WorkManager 15 分钟轮询通知
- ✅ App 打开时 3 秒轮询
- ✅ 通知渠道优化（震动、灯光、高优先级）

### 通知策略
| 场景 | 通知方式 | 延迟 |
|------|---------|------|
| App 打开 | WebView 3秒轮询 | 3秒 ✅ |
| App 后台 | WorkManager 15分钟 | 15分钟 ✅ |
| App 关闭 | WorkManager 15分钟 | 15分钟 ✅ |

---

## 🎯 结论

**15 分钟的通知延迟是可以接受的**：
- 这是亲子留言，不是即时聊天
- 只要联网，15 分钟内必定收到
- 不需要用户配置复杂权限
- 省电、稳定、可靠

**极光推送的 1-5 秒实时性并不值得**：
- 用户需要手动配置多项权限
- 华为设备限制严格，可能配置了也不工作
- 影响续航
- 增加维护成本

---

## 📝 已删除的极光推送相关代码

### Android
- `JPushReceiver.kt`
- `MainActivity.kt` 中的 `initJPush()`
- `android/app/libs/` 目录下的所有极光 SDK
- `AndroidManifest.xml` 中的极光配置

### 后端
- `server/app/jpush_util.py`
- `server/app/routes/micro_chat.py` 中的推送调用
- `server/requirements.txt` 中的 `httpx` 依赖

### 文档
- `JPUSH-INTEGRATION-SUMMARY.md`

---

**建议**：直接使用这个稳定版本（v2026.06.21.24），不需要极光推送。
