# 微聊原生增强 · 交接说明（StudyToolbox anchor APK）

> 交接日期：2026-09-10
> 来源机器：已完成"微聊图集（多图相册消息）"功能的网页端 + 服务端改造
> 接收机器：有 Android Studio / UU 模拟器的开发机（`C:\Ep\Code\AIvibeCoding\originInstallAPK`）
> 仓库：https://github.com/todimstar/study-toolbox-anchor

---

## 0. 给接收方 AI 助手的一页摘要

你要在 **StudyToolbox anchor（安卓壳 App）** 上做两件事：

1. **原生多图选择桥 `pickImages`**：让手机上一次从相册勾选多张图（系统 Photo Picker），替代网页 `<input type=file multiple>`（在国产 WebView 里只能一张张选）。协议完全模仿壳里**已有的原生录音桥**（见 §3）。
2. **原生级消息通知**：通知点击直达微聊界面、前台/后台通知可靠性、角标（见 §5）。

网页端（`toolbox.zakuku.top` 的 index.html）由来源机器负责接入兼容层，**你只需要在壳里实现桥**，按 §4 的协议返回 JSON 即可联调。壳的开发/调试用模拟器加载线上站点，无需部署服务器。

---

## 1. 系统全景

| 部分 | 位置 | 说明 |
|---|---|---|
| 前端 | `https://toolbox.zakuku.top`，服务器文件 `/var/www/toolbox.zakuku.top/index.html` | **单文件 HTML，CSS/JS 全部内联，无构建步骤**，改完即生效 |
| 后端 | Azure 服务器 `/opt/apps/study-toolbox/server`（FastAPI + SQLite） | systemd 服务名 `study-toolbox`，uvicorn 监听 `127.0.0.1:8000`，nginx 反代 `/api/` |
| 上传目录 | `/opt/apps/study-toolbox/server/uploads/micro-chat/` | 静态映射为 `/api/uploads/micro-chat/...` |
| 壳 App | 本仓库（anchor），WebView 加载上述站点 | 通过 `window.StudyToolbox` JS 桥与网页通信 |
| 双端身份 | 浏览器 = 家长"高人端"（parent）；壳 App 内 = 孩子"queen端"（child） | 网页用 `isToolboxApp` 判定：`!!(window.StudyToolbox && StudyToolbox.getAppVersion)` |

**当前 `window.StudyToolbox` 已实现的桥方法**（网页端已在调用，勿破坏）：

| 方法 | 返回 | 用途 |
|---|---|---|
| `getAppVersion()` | string | 存在性探测 + 版本显示 |
| `getClipboardText()` | string | 导入剪贴板 HTML |
| `setFullscreen(bool)` | - | 全屏运行器 |
| `showNotification(title, body)` | - | 通知栏通知 |
| `showChatBadge(title, body, messageId)` | - | 带角标的微聊通知（存在则优先于 showNotification） |
| `startNativeVoiceRecording()` | JSON 字符串 | 原生录音开始 |
| `stopNativeVoiceRecording()` | JSON 字符串 | 原生录音结束 |
| `cancelNativeVoiceRecording()` | JSON 字符串 | 原生录音取消 |
| `pickImages(limit)` | requestId 字符串 | 原生多图选择（回调式，结果经 `window.onToolboxImagesPicked` 返回）|

---

## 2. 网页端（微聊）近期已完成的功能（供理解上下文）

**微聊图集（多图相册消息）**，2026-09-09 上线：

- 后端新端点 `POST /api/micro-chat/messages/with-images`：multipart 字段 `sender_role / sender_name / sender_avatar / body` + 多个 `uploads[]` 文件；1–9 张；单张 ≤20MB；整批 ≤24MB；仅图片（MIME + magic bytes 双校验）；鉴权头 `X-Chat-Key`
- 数据模型：`micro_chat_messages.message_type = "gallery"`，新列 `attachment_urls`（TEXT，JSON 数组存放多个 URL）。旧单图消息仍是 `message_type="image"` + `attachment_url`
- 网页渲染：图集气泡（1 张大图 / 2–3 张 / 4+ 三列宫格）、全屏查看器（左右箭头循环、n/N 计数、双指缩放）
- 网页发送 UI：多选 → 预览条（"继续选图"按钮支持一次一张连选累积、9 张上限）→ 一条消息发送
- 测试图片附件 URL 形如 `/api/uploads/micro-chat/20260909140802_30c84a3a2b99a401.png`

> 即：**服务端与网页端已就绪**，手机端体验瓶颈只剩"每次只能从系统相册选一张"。这就是本交接要解决的。

---

## 3. 参考模板：壳里已有的"原生录音桥"协议

网页端调用与解析（index.html 内）：

```js
const nativeResult = parseBridgeJson(StudyToolbox.startNativeVoiceRecording());
if (nativeResult.ok) { ... }
if (nativeResult.reason === 'permission_requested') { ... }

// 结束录音：
const result = parseBridgeJson(StudyToolbox.stopNativeVoiceRecording());
// result = { ok: true, base64: "...", fileName: "voice.m4a", mime: "audio/mp4" }
await sendVoiceFile(base64ToFile(result.base64, result.fileName, result.mime));
```

要点：
- 桥方法**同步返回 JSON 字符串**；失败返回 `{ok:false, reason:"..."}`，权限类失败建议 `reason:"permission_requested"` 让网页提示用户
- 音频/图片**以 base64 过桥**是被验证可行的模式（`base64ToFile` 网页端已有，可直接复用）

---

## 4. 需求一：原生多图选择桥 `pickImages`

### 4.1 目标
App 内点"相册"时，弹出**系统原生多选界面**（可一次勾选多张，微信式体验），而不是 WebView 的单张文件选择器。

### 4.2 定稿协议（回调式 —— 因为系统 Photo Picker 是异步的）

> ⚠️ 关键差异：录音桥能"同步返回"是因为 start/stop 时数据已就绪；而选图必须等用户在系统界面操作完，**无法同步返回**。因此 `pickImages` 采用"调用 + 全局回调"两段式，网页端将按此对接（已排期实现回调处理）。

```js
// ① 网页调用（必须在用户手势的同步执行栈内调用，否则系统不弹窗）：
StudyToolbox.pickImages(9);          // 参数：最多可选张数（网页固定传 9）
// 返回：无（或返回 requestId 字符串，若壳支持多请求并发则返回；单请求场景可不返回）

// ② 原生在用户完成选择/取消后，通过 evaluateJavascript 回调网页：
window.onToolboxImagesPicked({
  ok: true,                          // 是否成功拿到图片
  images: [                          // 成功时存在
    { base64: "...", fileName: "IMG_0001.jpg", mime: "image/jpeg" },
    ...
  ],
  reason: "cancelled"                // 失败/取消时：cancelled | permission_requested | error
});
```

- 数量上限：**9 张**（与网页端一致，超出截断）
- 建议压缩到长边 ≤2560px、JPEG 质量 ~85 再 base64（服务端整批上限 24MB，桥传输也要控体量）
- 若壳暂时只有同步实现能力，也可先实现"单张同步返回"（`{ok:true, images:[一张]}`），网页端的"继续选图"连选可以兜底——但**推荐直接做回调式，一次到位**

### 4.3 安卓实现建议
- **API 33+（Android 13）**：`ActivityResultContracts.PickMultipleVisualMedia(9)`（系统 Photo Picker，零权限、原生多选 UI）
- **API 19–32 回退**：`ACTION_GET_CONTENT` + `Intent.EXTRA_ALLOW_MULTIPLE` + `ClipData` 读多选（部分国产 ROM 相册支持）；再不足退回 `ACTION_PICK` 单选，由网页端"继续选图"连选兜底
- 流程：JS 调用 → `@JavascriptInterface` 方法内启动 Activity → `ActivityResultLauncher` 回调拿到 URI 列表 → 读取/压缩/base64 → `webView.evaluateJavascript("window.onToolboxImagesPicked({...})")`
- 注意 `@JavascriptInterface` 方法内**不要做耗时读图**（会卡 WebView 线程），读图/压缩放后台线程，完成后回主线程 `evaluateJavascript`
- 若后续做 iOS 端：`PHPickerViewController`（`selectionLimit = 9`），回调协议不变（iOS 用 `WKScriptMessageHandler` 收到调用，`evaluateJavaScript` 回调同名字符串）

### 4.4 网页端接入计划（来源机器负责）
网页端将实现：
1. 全局 `window.onToolboxImagesPicked(payload)` 处理函数
2. `openImagePicker()` / `addMoreGalleryImages()` 中检测 `StudyToolbox.pickImages`：存在则调桥并进入"等待回调"态（按钮转 loading），收到回调后把 `images[]` 经 `base64ToFile` 转 `File`，汇入现有选图过滤/累积/预览/发送管线（`selectChatImages` → `sendGalleryMessage` → `POST /messages/with-images`）
3. 超时与异常兜底（如 60 秒未回调则恢复按钮并提示）；无 `pickImages` 桥时**自动退化为现有 Web input 方案**（渐进增强，壳不升级也不影响）
4. **壳侧无需关心上传逻辑**

---

## 5. 需求二：原生级消息通知

### 5.1 现状
- **前台（App 内）**：网页每 3 秒轮询一次新消息；发现新消息且是 App 环境时，调用 `StudyToolbox.showChatBadge(title, message, latestItem.id)`（无此桥方法则退化为 `showNotification(title, body)`）
- **后台**：服务器在**家长发消息**时通过极光推送（JPush）发通知（`send_jpush_notification`，仅 parent→child 方向）
- 网页入口支持 hash 直达面板：打开 `https://toolbox.zakuku.top/#chatPanel` 会直接渲染微聊面板（`openInitialPanel()` 已实现）——**deeplink 可直接复用此地址**

### 5.2 目标（请逐条核对实现/验证）
1. **通知点开直达微聊**：
   - App 内前台通知（showChatBadge）点击 → 打开/定位到微聊面板（网页侧可提供桥 `StudyToolbox` → WebView 内执行 `showPanel('chatPanel')`，或原生直接 loadUrl 追加 `#chatPanel`）
   - 后台 JPush 通知点击 → 打开 App 并直达微聊（极光后台配置打开 Activity / intent url 为 `https://toolbox.zakuku.top/#chatPanel`，或 App 内拦截后 loadUrl）
2. **前台收到消息**：保证通知栏通知 + 应用内红点/角标（现有 `showChatBadge` 若只是网页内 toast，请升级为真实通知栏 + 角标；网页已把 `messageId` 一并传入，可用于"已读清除角标"）
3. **通知渠道与权限**：Android 8+ `NotificationChannel`（建议单独"微聊消息"渠道，重要性 HIGH）；Android 13+ `POST_NOTIFICATIONS` 运行时权限申请与引导
4. **杀进程后**：依赖 JPush 通道（确认厂商通道已配，华为/小米/OPPO/vivo 等离线推送）
5. （可选）网页端在 App 内可以直接把轮询频率的职能交给壳？——**不建议改动**，保持现状即可

---

## 6. 接收机器的环境准备

### 6.1 修复 SSH `Bad permissions`（必做第一步）

报错：
```
Bad permissions. Try removing permissions for user: HPG4\CodexSandboxUsers (S-1-5-21-...)
on file C:/Users/hp1080/.ssh/config.
```

原因：OpenSSH for Windows 要求 `config` 与私钥文件**只允许 本人 / SYSTEM / Administrators** 访问；`CodexSandboxUsers` 组（agent 沙箱进程加的 ACE）导致校验失败。
在 PowerShell 中执行（对 config 和每个私钥文件都要做）：

```powershell
$sshdir = "$env:USERPROFILE\.ssh"
icacls "$sshdir\config" /inheritance:r
icacls "$sshdir\config" /grant:r "$env:USERNAME:(F)" "SYSTEM:(F)" "BUILTIN\Administrators:(F)"

# 对将要使用的私钥文件同样处理（例如 azure_zakuku_ed25519）：
icacls "$sshdir\azure_zakuku_ed25519" /inheritance:r
icacls "$sshdir\azure_zakuku_ed25519" /grant:r "$env:USERNAME:(F)" "SYSTEM:(F)" "BUILTIN\Administrators:(F)"
```

### 6.2 添加 azure 服务器连接（二选一）

**方案 A（推荐，更安全）— 本机新生成密钥，把公钥登记到服务器：**

1. 生成：`ssh-keygen -t ed25519 -f $env:USERPROFILE\.ssh\azure_zakuku_ed25519 -C "hp1080"`
2. 按 6.1 修好该私钥权限
3. 把生成的 `azure_zakuku_ed25519.pub` **一行内容**发给持有 Azure 服务器权限的人（来源机器侧负责追加到服务器 `~zakuku/.ssh/authorized_keys`）
4. 在 `C:\Users\hp1080\.ssh\config` 追加：

```
Host azure
    HostName 65.52.178.5
    User zakuku
    Port 22
    IdentityFile ~/.ssh/azure_zakuku_ed25519
```

**方案 B（快捷）— 直接复制来源机器的私钥** `azure_zakuku_ed25519`（连同 `.pub`）到 `C:\Users\hp1080\.ssh\`，按 6.1 修权限，config 同上。
⚠️ 私钥等同服务器密码，传输请走可信渠道（U 盘/加密压缩包），不要经聊天工具明文发送。

### 6.3 服务器常用操作（连上 azure 后）

```bash
# 重启后端（改 Python 后）
sudo systemctl restart study-toolbox && systemctl is-active study-toolbox

# 前端改动：直接覆盖 /var/www/toolbox.zakuku.top/index.html 即生效（无需构建）
# nginx 配置：/etc/nginx/sites-available/toolbox.zakuku.top；改后 sudo nginx -t && sudo systemctl reload nginx
# 日志
sudo journalctl -u study-toolbox -f
```

### 6.4 本地壳开发
- 仓库克隆后直接用 `C:\Ep\Code\AIvibeCoding\originInstallAPK` 对应的 Android Studio 打开编译
- 模拟器/真机运行壳，加载线上 `https://toolbox.zakuku.top` 即可联调（模拟器内是 child 端身份）
- 桥的调试：Chrome `chrome://inspect` 附加 WebView

---

## 7. 验收清单

- [ ] 真机/模拟器：App 内点「相册」→ 弹系统多选界面 → 一次勾选 3+ 张 → 预览条出现 → 发送 → 聊天里显示宫格消息
- [ ] 相册选图体验不劣化：单选也正常；取消不报错
- [ ] 家长（浏览器端）发消息 → 孩子 App 前台收到通知栏通知；点通知 → 直达微聊
- [ ] 家长发消息 → 孩子 App 后台/杀进程 → 收到极光推送；点推送 → 打开 App 并直达微聊
- [ ] 网页端原有功能回归：文字/语音/单文件/图集发送均正常；`showChatBadge` 签名不变或向后兼容

## 8. 敏感信息提醒

- 本说明**不含**任何私钥、服务器口令、聊天口令（`X-Chat-Key`）
- 服务器操作口令等按各自渠道单独交接

---

*文档由来源机器 Claude Code 生成于 2026-09-10。网页端（index.html）接入 `pickImages` 的兼容层已排期，桥实现完成即可联调。*
